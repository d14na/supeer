// const codecov = require('gulp-codecov')
const gulp = require('gulp')

gulp.task('default', function(_cb) {
    // place code for your default task here
    _cb()
})

// gulp.src('./coverage/lcov.info').pipe(codecov())
